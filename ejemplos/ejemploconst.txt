algoritmo alternativas;
ent a: entero;
sal a: entero;
{Prec: a= A}
var
    dinerosBanco, paga, regaloAcomprar: entero;
    comprarRegalo, deuda: bool;
fvar

dinerosBanco := 0;
paga := 1E3;
regaloAcomprar := -500;
comprarRegalo := falso;
deuda := false;

si dinerosBanco > 0 ->
    si paga > 0 ->
        si paga > regaloAcomprar ->
            comprarRegalo := verdadero;
            dinerosBanco := paga + regaloAcomprar
        fsi
    fsi
[] dinerosBanco <= 0 -> deuda := true;
fsi

{Post: a=A }
falgoritmo